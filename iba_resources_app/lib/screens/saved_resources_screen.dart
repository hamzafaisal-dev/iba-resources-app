import 'package:flutter/material.dart';

class SavedResourcesScreen extends StatelessWidget {
  const SavedResourcesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Saved Resources Screen'),
    );
  }
}
